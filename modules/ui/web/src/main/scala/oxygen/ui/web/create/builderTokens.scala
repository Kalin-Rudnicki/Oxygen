package oxygen.ui.web.create

/**
  * Shared builder tokens for HolyGrail-style widgets (W2-T01).
  */
enum Size {
  case ExtraSmall, Small, Medium, Large, ExtraLarge
}

enum Intent {
  case Primary, Accent, Neutral, Success, Warning, Danger, Info
}

enum ControlVariant {
  case Solid, Subtle, Minimal
}
