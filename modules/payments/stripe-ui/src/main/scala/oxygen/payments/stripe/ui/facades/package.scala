package oxygen.payments.stripe.ui

/**
  * Thin Scala.js facades over Stripe.js (loaded from CDN as `window.Stripe`).
  *
  * Minimal surface for collect-now (SetupIntent + Payment Element) and SCA recovery.
  */
package object facades
